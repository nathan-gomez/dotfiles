# Auth with polkit-gnome:
exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Start Keyring daemon
exec gnome-keyring-daemon --start --components=secrets,ssh,pkcs11

# Import environment variables for user systemd service manager
exec systemctl --user import-environment DISPLAY WAYLAND_DISPLAY SWAYSOCK

# Update dbus environments with display variables
exec hash dbus-update-activation-environment 2>/dev/null && \
     dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY SWAYSOCK

# XDG desktop portal (needed for GTK4/libadwaita dark mode and file choosers)
exec systemctl --user start xdg-desktop-portal-gtk xdg-desktop-portal

# Desktop notifications
exec mako

# Start foot server
exec_always foot --server

# Autotiling
exec autotiling

# cliphist
exec wl-paste --type text --watch cliphist store
exec wl-paste --type image --watch cliphist store

# Network Applet
exec nm-applet --indicator

# Firewall Applet
exec sleep 2 && firewall-applet

# Set cursor
seat seat0 xcursor_theme Bibata-Modern-Ice
